# 更新日志（CHANGELOG）

## 0.1.4（2026-09-02）

需求迭代：**扩充短期任务范围**——「用户每次让会话去做某件事」即一条短期任务；执行过程里的操作不算任务。

### 变更
- `task-panel:task-convention` 提示词段重写短期规则（0.1.2 约定的扩充实化）：
  - 新规则 1-2：用户每次让会话「去做某件事」= 一条短期任务；一句话含多件事拆多条；共同商定要做的事同样计入。
  - 新规则 3：任务以「用户要做的事」为粒度，**具体执行过程里的操作不算任务**（安装工具、生成密钥、运行命令、语法检查、验证预检、清理旧副本、写测试、汇总汇报等）。
  - 新规则 4：用户的问题、咨询、确认语（「好了」「可以」「谢谢」）不属于让会话做事，不记录为任务。
  - 长期规则编号顺延（6-9），内容不变。
- 冒烟测试约定断言更新（校验「去做某件事」与「执行过程里的操作不算任务」两条新规则存在）。
- README（简介、功能表、4.3 约定、FAQ Q3/Q4）与 docs/ARCHITECTURE.md（§3.3、§4.4）同步。
- 版本号 0.1.4。

---

## 0.1.3（2026-09-02）

需求迭代：长期任务定义调整——**长期任务 = 会话中明确委派/共同商定的长期任务（即目标 goal）；子代理不再展示**（子代理、后台任务、工作流视为 AI 内部执行机制，与工具调用同类）。

### 变更
- **宿主端**：移除 `subagents` 服务读取与响应字段（`collectState` 只返回 session/goal/todos；空参数与 500 分支同步清理）；约定提示词段更名为 `task-panel:task-convention`，内容扩展为短期+长期两份规则（长期：目标只对应用户明确要求/共同商定的长期目标；一个用户可见长期任务=一个目标；不为内部子目标/分段创建目标；子代理等内部机制不创建目标、不写入清单）。
- **客户端**：移除子代理行渲染（SubagentRow 组件、subagent 排序、长期组「已完成」折叠开关 showDoneLong、子代理派生逻辑）；「长期任务」组仅显示目标卡；busy 判定只保留待办与目标。
- **冒烟测试**：删除子代理构造与断言；新增「响应不包含 subagents 字段」断言；约定断言更新为 `task-panel:task-convention` 并校验长短规则均存在。

### 文档
- README：功能一览、界面示意、字段含义、数据流、响应示例、关键规则、架构图、FAQ Q8（改为「为什么看不到子代理/后台任务/工作流」）、开发者指南测试清单全部同步。
- docs/ARCHITECTURE.md：数据契约（Schema 去掉 subagents）、语义要点（新增长期粒度与内部机制不展示）、服务读取矩阵（去掉 subagents、加 systemPrompt）、§4.4 改为「任务粒度约定」、时序图、测试清单、扩展点同步。

---

## 0.1.2（2026-09-02）

需求迭代：短期任务定义调整——**短期任务 = 会话中明确委派/共同商定的任务（用户级粒度），代理的内部操作步骤不计入**。

### 新增
- 宿主端注册系统提示词段 `task-panel:todo-convention`（order 116）：向 profile 内所有会话的代理声明
  「一条 todo = 一个用户可见任务」约定，并给出用户侧示例（「将该插件移入 D:\...，并撰写文档」
  → 两条任务：`移动插件文件位置`、`撰写介绍文档`），明确排除内部执行步骤（建目录、备份、
  安装命令、语法检查、验证预检、清理旧副本、汇总汇报等）。
- smoke.mjs 增加约定注册断言（提示词段名称与内容校验）。

### 文档
- README 第 1/2/4.3/8 节同步新定义（含 FAQ Q3/Q4 粒度说明）。
- docs/ARCHITECTURE.md 新增 §4.4（粒度约定机制）并更新数据契约语义表。

---

## 0.1.1（2026-09-02）

需求迭代：折叠态改为鲸鱼图标；刷新频率优化。

### 新增
- 折叠按钮由 22px 小圆点升级为 **46px 圆形按钮 + 🐳 鲸鱼图标（26px）**；有任务运行时按钮右上角显示闪烁小蓝点（依旧无数字角标）。
- 页头新增 **「↻」手动刷新按钮**：立即拉取最新任务数据，并重置一分钟轮询倒计时。
- 面板底部新增 **「更新于 今天 HH:MM」**：最近一次成功获取数据的时刻，便于确认数据新鲜度。

### 变更
- 自动刷新间隔 **1 秒 → 60 秒**（`POLL_MS = 60000`），显著降低请求频率（每分钟仅 1 个极小 JSON 请求）。
- 页头流量提示文案由「每 1 秒刷新」改为「每 1 分钟刷新」。
- 文档补全：README（使用/FAQ）、docs/ARCHITECTURE.md（技术设计）。

### 修复
- 修复 GoalCard 底部时间行拼接 React 元素导致显示 `[object Object]` 的问题。
- 补充缺失的 `@keyframes dshTaskPanelPulse` 动画定义（此前仅引用未定义）。

---

## 0.1.0（初始版本）

需求调研结论落实（三轮确认需求：任务来源=Harness 自动跟踪且仅当前会话；短期=待办，长期=目标+子代理；
界面=右下角可拖动悬浮窗，中文，纯查看，已完成置灰可折叠，进行中优先排序）。

### 新增
- **Host 端（lib/index.js）**：注册 `/plugins/dsh-task-panel/state` 只读路由；
  折叠 `todo/write` 会话事件得到当前待办（复刻官方投影的 turn/start 重置语义）并推导每项开始/完成时间；
  读取 `goals` 服务当前目标视图（轮数、阶段、受阻原因、时间戳）；
  枚举 `subagents` 直接子代理（活动状态、模式、创建时间）。
  全部服务可选读取 + try/catch 降级；零写入、零硬依赖。
- **Client 端（lib/client.js）**：经 `dsh.client` 元数据与 `window.__ModuleLoader__` 协议随 Web 应用挂载；
  React 门户悬浮窗（右下角、可拖动、可折叠）；短期/长期两组展示；组进度条与目标轮数进度条；
  状态标签、开始时间、已用时长；已完成置灰可折叠「已完成 N 项」；
  折叠态不自动弹出；偏好（展开/折叠、位置）存 localStorage。
- **工程**：`cordis.patch.yml` 组合补丁；`smoke.mjs` 宿主冒烟测试；中文文档体系。
- 安装：`dsh plugin --profile web add <path>`（link: 目录链接，源码改动重启即生效）。
